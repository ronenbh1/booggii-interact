/** @jsxImportSource @emotion/react */
import Page from '../layout/Page'

const styles = {
  root: theme => ({}),
}

const Activity = () => <Page name="activity" css={styles.root}></Page>

export default Activity
