/** @jsxImportSource @emotion/react */
import Page from '../layout/Page'

const styles = {
  root: theme => ({}),
}

const Dashboard = () => <Page name="dashboard" css={styles.root}></Page>

export default Dashboard
